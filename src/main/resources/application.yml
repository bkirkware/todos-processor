spring:
    cloud:
        stream:
            kafka:
                binder:
                    brokers: 'kafka.pks.kirklab.io'
            bindings:
                input:
                    destination: todos
                output:
                    destination: todos-out
