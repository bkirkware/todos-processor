---
applications:
- name: todos-processor
  memory: 1G
  routes:
  - route: todos-processor.apps.pas.kirklab.io
  path: target/todos-processor-1.0.0-SNAP.jar
  buildpack: java_buildpack_offline
  services:
  - todos-messaging
