spring:
    cloud:
        stream:
            kafka:
                binder:
                    brokers: 'kafka.retro.io'
            bindings:
                input:
                    destination: todos
                output:
                    destination: todos-out